package com.ubopod.uboapp.wear

import android.app.Application

/**
 * Custom [Application] entry point for the Ubo wear app.
 *
 * Mirrors `ubo-swift-app/ubo Watch App/uboApp.swift` — global one-time
 * setup goes here (logging, fonts, …). Kept minimal in v1.
 */
public class UboWearApplication : Application()
